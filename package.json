{
  "name": "yaca",
  "version": "1.0.4",
  "description": "Yet Another Collection Approach",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "documentation": "typedoc --out ./docs --readme README.md --target ES5 --name 'Yet Another Collection Approach' && cp .nojekyll ./docs/.nojekyll",
    "prepublish": "npm run build",
    "build": "tsc && npm run documentation",
    "test-mocha": "mocha -r ts-node/register ./test/**/*Test.ts",
    "test": "nyc --reporter=html --reporter=text mocha -r ts-node/register --timeout=3000",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "nyc": {
    "extension": [
      ".ts",
      ".tsx"
    ],
    "exclude": [
      "**/*.d.ts",
      "**/*Test.ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rabanti-github/yaca.git"
  },
  "keywords": [
    "TypeScript",
    "Collections",
    "List",
    "Dictionary",
    "ArrayList",
    "Generics"
  ],
  "author": "Raphael Stoeckli <npm@rabanti.ch> (https://github.com/rabanti-github)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/rabanti-github/yaca/issues"
  },
  "homepage": "https://github.com/rabanti-github/yaca#readme",
  "devDependencies": {
    "@types/chai": "^4.0.1",
    "@types/mocha": "^2.2.41",
    "@types/node": "^8.0.8",
    "chai": "^4.1.0",
    "coveralls": "^2.13.1",
    "istanbul": "^0.4.5",
    "mocha": "^3.4.2",
    "nyc": "^11.0.3",
    "ts-node": "^3.2.0",
    "tslint": "^5.5.0",
    "typedoc": "^0.7.1",
    "typescript": "^2.4.1"
  },
  "dependencies": {
    "lodash.isequal": "^4.5.0"
  }
}
